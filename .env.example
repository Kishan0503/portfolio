# GitHub classic PAT with read:user scope — for the contribution graph (GraphQL).
# Server-side only (Vercel env var); never exposed to the browser.
GITHUB_TOKEN=

# Resend API key — for the contact form email function.
# Server-side only; emails submissions to the owner.
RESEND_API_KEY=
